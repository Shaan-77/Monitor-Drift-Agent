# zeroui-simulator-owned
name: ZeroUI FM-1 Simulator ER-028 Skipped

on:
  pull_request:
    branches:
      - main
    paths:
      - ".zeroui-simulator/er028/skipped/**"

permissions:
  contents: read

jobs:
  fm1-er028-skipped:
    name: zeroui-fm1-er028-skipped
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: This step must not execute
        run: echo "skipped"
