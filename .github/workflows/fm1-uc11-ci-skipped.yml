name: FM-1 ER-028 check failed
on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  fm1-intentional-failure:
    name: fm1-check-failed
    runs-on: ubuntu-latest
    steps:
      - name: Fail on purpose
        run: echo ok
