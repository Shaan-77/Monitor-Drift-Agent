[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "monitor-drift-agent"
version = "0.1.0"
description = "ER-070 controlled artifact build failure fixture"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
